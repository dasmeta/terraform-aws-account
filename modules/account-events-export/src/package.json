{
  "name": "account-events-export",
  "private": true,
  "scripts": {
    "build": "rm -f ../account-events-export-lambda.zip && zip -X -q ../account-events-export-lambda.zip README.md index.js package-lock.json package.json",
    "test": "node --test index.test.js"
  }
}
